# Architecture

> Auto-generated by /map on 2026-04-11

## Overview

Pathfinder is a career-guidance platform designed to help users discover their professional path through interactive assessments. It features a modern, responsive React-based frontend with real-time data persistence and authentication provided by Supabase.

```
┌─────────────────────────────────────────┐
│              [main.tsx]                 │
├─────────────────────────────────────────┤
│         [App.tsx (Routing)]             │
├─────────────────────────────────────────┤
│    [Pages (Quiz, Results, Auth, etc.)]  │
├─────────────────────────────────────────┤
│ [integrations/supabase] <--> [Supabase] │
└─────────────────────────────────────────┘
```

## Components

### Pages
- **Auth:** Handles user login and registration using Supabase Auth.
- **Quiz:** The core interactive assessment engine.
- **Results:** Visualizes assessment outcomes using Recharts.
- **Dashboard:** User-specific overview and progress tracking.
- **Install:** PWA/Installation flow for mobile-like experience.

### Shared
- **integrations/supabase:** Centralized client configuration and TypeScript definitions.
- **components/ui:** Reusable UI primitives based on shadcn/ui.
- **hooks:** Custom logic for data fetching and application state.

## Data Flow

1. **User Interaction**: User engages with the Quiz or Dashboard.
2. **State Management**: Local UI state via React, remote state via TanStack Query.
3. **External Sync**: Hooks trigger Supabase client calls to fetch/store user response and auth data.
4. **Output**: Results are dynamically rendered and visualized using Recharts and Framer Motion.

## Integration Points

| Service | Type | Purpose |
|------|------|---------|
| Supabase | Auth / DB | User authentication and persistence of quiz results. |
| Vercel | Deployment | Production hosting and CI/CD (based on `vercel.json`). |

## Technical Debt

- [ ] Consolidate Supabase types (observed `types.ts` and `client.ts` separation).
- [ ] Implement robust error boundaries for the Quiz engine.
- [ ] Add unit tests for assessment logic (found `src/test` but limited coverage observed).

## Conventions

**Naming:** PascalCase for components, camelCase for hooks and utilities.
**Structure:** Feature-based page organization with a shared `components/ui` layer.
**Testing:** Vitest with React Testing Library.
